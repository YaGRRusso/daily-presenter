services:
  db:
    image: mongo:7-jammy
    ports:
      - "27017:27017"
    volumes:
      - presenters:/data/db

  backend:
    depends_on:
      - db
    build: .
    ports:
      - "3000:3000"
    environment:
      - DATABASE_URL=mongodb://db:27017/presenters
      - JWT_SECRET=brigadeiro
      - HOST=0.0.0.0
      - PORT=3000
    # env_file:
    #   - .env
    command: npm run start:prod

volumes:
  presenters:
